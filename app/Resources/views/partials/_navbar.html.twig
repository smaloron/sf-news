<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle"
                    data-toggle="collapse"
                    data-target="#nav-content">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">Symfony News</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="nav-content">
            <ul class="nav navbar-nav">
                <li>
                    <a href="{{ path('article_list') }}">Tous les articles</a>
                </li>
                <li>
                    <a href="{{ path('about_page') }}">A propos</a>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('author_home') }}"><span class="glyphicon glyphicon-log-in"></span> Accès auteur</a></li>
                <li><a href="{{ path('admin_home') }}"><span class="glyphicon glyphicon-cog"></span> Admin</a></li>
            </ul>
        </div>
    </div>
</nav>